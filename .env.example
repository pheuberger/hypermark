# Hypermark services URL (signaling + suggestion API run on same server)
# For local development (default):
VITE_SIGNALING_URL=ws://localhost:4444

# For ngrok tunneling (example):
# VITE_SIGNALING_URL=wss://your-server.ngrok-free.app

# For production (self-hosted):
# VITE_SIGNALING_URL=wss://your-server.fly.dev

# Content Suggestion Service URL (optional)
# If not set, derives from VITE_SIGNALING_URL (same server)
# VITE_SUGGESTION_URL=http://localhost:4444
# VITE_SUGGESTION_URL=https://your-server.fly.dev
